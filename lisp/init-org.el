(global-set-key (kbd "<f12>") 'org-agenda)
(global-set-key (kbd "<f8>") 'org-cycle-agenda-files)

; Show its table-of-content when a org file is opened
(setq org-startup-folded 'content)

(provide 'init-org)
